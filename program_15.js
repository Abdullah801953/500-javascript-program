function countWord(str) {
  for (let i = 0; i < str.length; i++) {
    if (str[i] === " ") {
        
    }
  }
}
const str = "This is a sentance for test program";
const output = countWord(str);
console.log(output);
